# Notebook

`01_end_to_end_analysis.ipynb` is a lightweight walkthrough that calls the tested package pipeline, then displays the generated metrics and figures. The production logic remains in `src/` so notebook execution and application inference use the same implementation.

